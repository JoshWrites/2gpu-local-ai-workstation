# Code mode — memory-bank rules (template)

Rules governing how Code mode reads and updates the project's `memory-bank/`.

TODO: Populate with memory-bank protocol — which files to read at session start,
when to append to `activeContext.md` / `progress.md` / `decisionLog.md`, when to
promote a recurring pattern into `systemPatterns.md`.
