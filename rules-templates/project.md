# Project rules (template)

Per-project rules for Roo Code. Copy into a new project's `.roo/rules/project.md`
and customize.

TODO: Fill in project-specific conventions (stack, test runner, naming, CI gates,
commit style, any forbidden patterns).
